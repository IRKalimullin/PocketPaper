package com.baleshapp.pocketpaper.settings

/**
 * Enum class contains application theme modes
 *
 * AUTO - automatically (system theme)
 *
 * LIGHT - light theme mode;
 *
 * DARK - dark theme mode
 */
enum class ThemeModes {
    AUTO,
    LIGHT,
    DARK
}